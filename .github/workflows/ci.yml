name: CI

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  style:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Lean style (100-col / trailing-ws / tabs)
        run: python3 scripts/style-check.py
      - name: Git whitespace
        run: git diff --check

  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Cache elan toolchain
        uses: actions/cache@v6
        with:
          path: ~/.elan
          key: elan-${{ runner.os }}-${{ hashFiles('lean-toolchain') }}
      - name: Cache build
        uses: actions/cache@v6
        with:
          path: .lake
          key: lake-${{ runner.os }}-${{ hashFiles('lake-manifest.json', 'lakefile.lean', 'lean-toolchain') }}
          restore-keys: lake-${{ runner.os }}-
      - name: Fetch pinned Lake dependencies
        env:
          GH_TOKEN: ${{ secrets.ECOSYSTEM_READ_TOKEN || github.token }}
        run: |
          set -euo pipefail
          mkdir -p .lake/packages
          if [ ! -d .lake/packages/termcolor/.git ]; then
            git clone --depth=1 "https://x-access-token:${GH_TOKEN}@github.com/jonaprieto/lean-termcolor.git" \
              .lake/packages/termcolor
          fi
          if [ ! -d .lake/packages/termcolor-layout/.git ]; then
            git clone --depth=1 "https://x-access-token:${GH_TOKEN}@github.com/jonaprieto/lean-termcolor-layout.git" \
              .lake/packages/termcolor-layout
          fi
          git -C .lake/packages/termcolor fetch --depth=1 origin \
            d0d9a6d24cd06a2ca5de0c47d1860fb0363aa397
          git -C .lake/packages/termcolor-layout fetch --depth=1 origin \
            de2394b9e1bd2328e3577115ab4b75fd1d8faf8e
          git -C .lake/packages/termcolor checkout --detach \
            d0d9a6d24cd06a2ca5de0c47d1860fb0363aa397
          git -C .lake/packages/termcolor-layout checkout --detach \
            de2394b9e1bd2328e3577115ab4b75fd1d8faf8e
      - uses: leanprover/lean-action@v1
        with:
          build-args: "TermColor.Diagnostics TermColor.Diagnostics.Properties tests detect readme demo"
      - name: Run tests
        run: ./.lake/build/bin/tests
      - name: Verify terminal policies
        run: |
          env -u NO_COLOR -u FORCE_COLOR TERM=xterm ./.lake/build/bin/detect plain
          env -u NO_COLOR FORCE_COLOR=1 TERM=xterm ./.lake/build/bin/detect ansi16
          env -u NO_COLOR FORCE_COLOR=1 TERM=xterm-256color ./.lake/build/bin/detect ansi256
          env -u NO_COLOR FORCE_COLOR=1 TERM=xterm COLORTERM=truecolor \
            ./.lake/build/bin/detect truecolor
          env NO_COLOR=1 FORCE_COLOR=1 TERM=xterm ./.lake/build/bin/detect plain
          env -u NO_COLOR FORCE_COLOR=1 TERM=dumb ./.lake/build/bin/detect plain
      - name: Verify README examples
        run: ./.lake/build/bin/readme >/dev/null
      - name: Render demo gallery
        run: ./.lake/build/bin/demo >/dev/null
      - name: Audit proof axioms
        run: python3 scripts/check-axioms.py
